<?xml version="1.0" encoding="UTF-8"?>

<!-- mapper 기본설정 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.todo.mapper.InterBoardMapper"> 

	<insert id="registerMethod" parameterType="HashMap">
		insert into tbl_user(userid, email, passwd, name, mobile)
		values(#{userid}, #{email}, #{passwd}, #{name}, #{mobile})
	</insert>

</mapper>